name: Dialect Staging

on:
  push:
    branches: [ "dialect-staging" ]

jobs:
  dialect-staging:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate Dialect Staging
        run: |
          echo "🌀 Dialect Staging Gate Engaged"
          if grep -q "TODO" docs/DialectManifest.md; then
            echo "❌ Dialect Manifest in staging contains TODO placeholders."
            exit 1
          else
            echo "✅ Dialect Manifest staging clean."
          fi
